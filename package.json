{
  "name": "tictactoe-infinite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server --no-audit && npm install --prefix client --no-audit",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c cyan,magenta --kill-others-on-fail \"npm:dev:server\" \"npm:dev:client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm test --prefix server && npm test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
